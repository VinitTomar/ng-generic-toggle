import { CheckedStateDirective } from './checked-state.directive';

describe('CheckedStateDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckedStateDirective();
    expect(directive).toBeTruthy();
  });
});
