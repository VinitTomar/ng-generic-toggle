import { UnCheckedStateDirective } from './un-checked-state.directive';

describe('UnCheckedStateDirective', () => {
  it('should create an instance', () => {
    const directive = new UnCheckedStateDirective();
    expect(directive).toBeTruthy();
  });
});
